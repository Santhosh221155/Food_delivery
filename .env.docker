# Docker Environment Configuration
# Used by docker-compose.yml for all services

# =====================================================
# MongoDB Configuration
# =====================================================
MONGO_INITDB_ROOT_USERNAME=santhosh_db_user
MONGO_INITDB_ROOT_PASSWORD=san221155
MONGO_INITDB_DATABASE=fooddelivery_prod
MONGODB_URI=mongodb://santhosh_db_user:san221155@mongodb:27017/fooddelivery_prod?authSource=admin

# =====================================================
# Node.js API Gateway Configuration
# =====================================================
NODE_ENV=production
PORT=3000
JWT_SECRET=your-super-secret-jwt-key-change-in-production-2024
JWT_EXPIRE=7d
CORS_ORIGIN=http://localhost
USE_CLUSTER=false
NUM_WORKERS=2

# Python Backend base URL (internal Docker network)
DOWNSTREAM_BASE_URL=http://python-api-1:5000

# =====================================================
# Python FastAPI Configuration
# =====================================================
ALLOWED_ORIGIN=http://localhost

# =====================================================
# Frontend Configuration
# =====================================================
VITE_API_URL=http://localhost/api

# =====================================================
# Nginx Configuration
# =====================================================
# Load balancer listens on port 80 (HTTP)
# Routes to Node.js instances (node-api-1, node-api-2, node-api-3)
NGINX_PORT=80
NGINX_SSL_PORT=443
